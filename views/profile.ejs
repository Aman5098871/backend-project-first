<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <form method="get" action="/logout" class="flex width-full justify-end">
        <a class="bg-red-500 rounded-md text-sm px-3 py-2 inline-block mb-5" href="/logout"> LOGOUT</a>
        
      </form>
      <h3 class="text-3xl">
        <span class="font-light">Hello,</span> <%= user.name%> 👋
      </h3>
      <h5 class="mb-5">You can create new post.</h5>
      <form action="/post" method="post">
        <textarea
          placeholder="What's on Your Mind"
          class="block p-3 bg-transparent outline-none rounded-md border-2 border-zinc-800 resize-none w-1/3"
          name="content"
        ></textarea>
        <input
          class="block px-3 py-2 bg-blue-500 rounded-md mt-5 w-40 text-sm"
          type="submit"
          value="Create New Post"
        />
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
          <% user.posts.reverse().forEach((post)=>{ %>
        <div class="post w-1/3 p-4 border-[1px] border-zinc-700 rounded-md bg-zinc-800 mb-4">
            <h4 class="text-blue-500 mb-2"> @<%= user.username %></h4>
            <p class="text-sm trcking-tighter"> <%= post.content %></p>
            <small class="mt-2 inline-block"> <%= post.likes.length %> likes</small>
            <div class="btns flex gap-5 mt-1">
              <a class="text-blue-500" href="/like/<%= post._id %>">
                <%= post.likes.indexOf(user._id)=== -1 ? "like" : "unlike"%>
            </a>
              <a class="text-red-500" href="/edit/<%= post._id %>">Edit</a>
            </div>
          </div>
         <% })%>
        </div>
      </div>
    </div>
  </body>
</html>
