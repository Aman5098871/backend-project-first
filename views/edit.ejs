<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="flex width-full justify-end"">
        <a class="bg-red-500 rounded-md text-sm px-3 py-2 inline-block mb-5" href="/logout"> LOGOUT</a>

    </div>
        
      </form>
      <h5 class="mb-5">Edit Your posts.</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea placeholder="What's on Your Mind" class="block p-3 bg-transparent outline-none rounded-md border-2 border-zinc-800 resize-none w-1/3" name="content"><%= post.content %></textarea>
        <input class="block px-3 py-2 bg-yellow-500 text-black rounded-md mt-5 w-40 text-sm" type="submit" value="Update Post"/>
      </form>
    </div>
  </body>
</html>
